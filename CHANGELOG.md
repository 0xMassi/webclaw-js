# Changelog

## Unreleased

- **Breaking:** Lead enrichment retired. `lead`, `leadBatch`, `getLeadBatch`, `waitForLeadBatch` and the `Lead*` request/response types are removed. Use `extract` with your own schema for structured page extraction.
